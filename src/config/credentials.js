/*import React from "react";
const loginCredentials = localStorage.getItem("lbzapp");
if (loginCredentials != null) {
  var logindata = JSON.parse(loginCredentials);
} else {
  var logindata = {};
}

export const ApiToken = logindata.apiToken;
export const AuthToken = logindata.authKey;*/
